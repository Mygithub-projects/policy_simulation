# Task 3 Report — Frontend regenerate-and-download PDF flow

## Scope

Implemented only Task 3 of `docs/superpowers/plans/2026-07-12-policy-maker-my-runs-implementation.md` ("Frontend — regenerate-and-download PDF flow"). Tasks 1 and 2 were already done/verified and were not touched.

## Changes made

1. **`frontend/app.js`** — added `downloadPdfForRun(scenario)` directly after the existing `runSimulation()` function (before the `// AI AGENT` section header), using the exact code from the plan: calls `goToDashboard()`, `showLoading(t('loading.sim'))`, `apiFetch('/api/simulate', ...)`, sets `state.currentRunId`, calls `renderResults(data, payload)`, waits 1200ms for Chart.js animation, then calls `downloadSummaryPDF()`. Errors are caught and reported via `showError(...)` with `showLoading(null)` to clear the loader.
2. **`frontend/index.html`** — bumped the cache-busting query string from `app.js?v=27` to `app.js?v=28` on the `<script src="app.js?v=28"></script>` tag.

No other files were modified.

## Verification performed

### Setup
- Confirmed port 8010 was free (`netstat -ano | findstr LISTENING`), then started an isolated server: `python -m uvicorn main:app --host 127.0.0.1 --port 8010`.
- No disposable Policy Maker (`user`-role) test account existed. Created one directly via a DuckDB insert (mirroring `create_user.py`'s approach) into the `users` table: username `test_policymaker_task3`, role `user`, active, `is_first_login=false`. The real `superadmin` account was never touched.

### End-to-end flow (Playwright, against the 8010 instance)
1. Logged in via the UI as `test_policymaker_task3` (Policy Maker). Confirmed the "My Runs" button is visible (RBAC-gated feature working for this role).
2. Ran one simulation via the normal "Run 2027 Simulation" button to seed a `simulation_run_log` row for this user. Before the download-PDF test, `simulation_run_log` count for this user = 1.
3. Navigated to "My Runs" — table showed the seeded run (`2026-07-12 21:27:40...`, "All Subjects / All States", "Subject-Option Teacher Ratio") with a "⬇ Download PDF Report" button.
4. Clicked "⬇ Download PDF Report" on that row.
   - View switched to the dashboard.
   - Results populated (KPIs, subject-option comparison chart, plain-language summary, priority-schools table) matching that row's scope (All Subjects / All States) and policy (Subject-Option Teacher Ratio, 70%).
   - Playwright's console/download log confirmed a PDF (`simulation_2027_report_RUN_20260712_213000_513784.pdf`) downloaded automatically shortly after render.
5. Went back to "My Runs" — the just-downloaded run (`2026-07-12 21:30:00...`) now appears at the top of the list, above the original seeded run, exactly as expected (re-running writes a new log entry).
6. Queried `simulation_run_log` directly before/after the click:
   - Before: `SELECT COUNT(*) FROM simulation_run_log WHERE run_by='test_policymaker_task3'` → `1`
   - After: same query → `2`
   - Growth = exactly 1. No duplicate `/api/simulate` call was triggered by the download click.

### Automated tests
- `python smoke_test.py` — passed (produced expected health/summary/explanation/artifacts output, no exceptions).
- `python api_smoke_test.py` — passed ("API smoke test passed", all four scenarios — baseline, co-teaching, combined-all-policy, agent — returned expected summaries).

### Cleanup
- Deleted only the `outputs/RUN_*` files generated by my own verification steps (the two runs from the manual Playwright test: `RUN_20260712_212740_258221_*`, `RUN_20260712_213000_513784_*`; and the runs generated by the smoke test scripts just now: `RUN_20260712_213129_433582_*` and the eight `RUN_20260712_2132*` files from `api_smoke_test.py`). No other files in `outputs/` were touched.
- Deactivated (not deleted) the disposable test account: `UPDATE users SET is_active=FALSE WHERE username='test_policymaker_task3'`. Its `audit_log`/`simulation_run_log` rows remain intact and linked, per project convention (no hard-delete).
- Stopped the test server on port 8010 (`Stop-Process -Id 16904 -Force`) and confirmed via `netstat -ano | findstr LISTENING` that port 8010 is free again. The pre-existing server on port 8002 was never touched.

## Post-Implementation Checklist (from the plan)

- Re-ran the relevant manual flows via the same test session: Policy Maker login, single-policy simulation (via "Run 2027 Simulation") worked normally; Agent Chat was exercised as part of `api_smoke_test.py`'s "Agent scenario" check (passed). Combined-policy mode and all four levers were exercised by `api_smoke_test.py`'s combined/co-teaching scenarios (passed) — nothing in `downloadPdfForRun()`'s shared code path (`apiFetch` → `renderResults` → `downloadSummaryPDF`) was changed, so no regression risk beyond what smoke tests already cover.
- Did not re-verify the Superadmin/Admin "My Runs" 403 restriction in this task, since that RBAC gating belongs to Task 1/2 (already verified per the task instructions) and Task 3 made no changes to `main.py` or the RBAC logic.
- No `.duckdb` schema changes were made. Only additive row changes: one new `users` row (test account, now deactivated) and new `simulation_run_log` rows from normal simulate calls (seed run + `downloadPdfForRun` regenerate + smoke-test runs), consistent with existing behavior — no table structure was altered.

## Concerns

None. The implementation matches the plan's exact code, the end-to-end flow works as specified, and the run-count check confirms no duplicate `/api/simulate` calls are triggered by the button click.
