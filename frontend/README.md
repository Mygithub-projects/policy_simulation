# Frontend placeholder

Build the POC frontend here later using:

- `index.html`
- `styles.css`
- `app.js`

The JavaScript frontend will call the FastAPI base URL:

```text
http://127.0.0.1:8002
```

See `API_CONTRACT.md` in the project root for endpoints and example `fetch()` calls.
